# 核心依赖（必需）
PyQt5>=5.15.0
PyQt5-sip>=12.9.0
pyttsx3>=2.90
PyAudio>=0.2.11
loguru>=0.7.0
chardet>=5.0.0

# 音频处理依赖（可选，用于预览功能支持更多音频格式）
# mutagen 是轻量级的音频元数据读取库，推荐安装
mutagen>=1.47.0

# 其他可选的音频处理库（任选其一即可）
# soundfile>=0.12.1
# audioread>=3.0.0
# pydub>=0.25.1
